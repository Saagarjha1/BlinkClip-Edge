<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>BlinkClip | Login / Signup</title>

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;600;700;800&display=swap" rel="stylesheet">

  <!-- Favicon -->
  <link rel="icon" href="/icon.png" type="image/png"/>

  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Manrope', sans-serif;
      background-color: #ffffff;
      display: flex;
      min-height: 100vh;
      flex-direction: column;
    }

    .container {
      display: flex;
      flex-direction: column;
      height: 100vh;
    }

    @media (min-width: 768px) {
      .container {
        flex-direction: row;
      }
    }

    .image-section {
      flex: 1;
      background-image: url('https://lh3.googleusercontent.com/aida-public/AB6AXuCf6xjRcmUhvuUemt0W85pWoRu4JaX9kTalM30k_8yK6X6n5PY5yueMYlQcmsNBVn5nN0ojpeyMsdz-NY2vk1YUPBJlAXWD6SBiIwyGLjk196X5Vdu1NHoEdLl0vyXUcwvldgE_G4VXJ1yDiXB5wDCazn9mMWl7ry32T0k3z3v5rbbNnnZBqPgAyCD-j083EsZNngHC-_PelH362obqPaGa-9V_PClkaOhJ2WUt9wYttxzgrGbIjol-adANslaXUD2-HkGPqEIZB-4');
      background-size: cover;
      background-position: center;
      height: 250px;
    }

    @media (min-width: 768px) {
      .image-section {
        height: auto;
      }
    }

    .form-section {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 2rem;
    }

    .form-wrapper {
      width: 100%;
      max-width: 400px;
    }

    h2 {
      font-size: 2rem;
      font-weight: 700;
      text-align: center;
      margin-bottom: 1.5rem;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    input {
      padding: 0.75rem;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 1rem;
    }

    button[type="submit"] {
      padding: 0.75rem;
      background-color: #0c77f2;
      color: white;
      font-weight: 600;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 1rem;
    }

    button[type="submit"]:hover {
      background-color: #075dd4;
    }

    .toggle-container {
      margin-top: 1rem;
      text-align: center;
      color: #555;
    }

    .toggle-container button {
      background: none;
      border: none;
      color: #0c77f2;
      font-weight: 600;
      cursor: pointer;
      text-decoration: underline;
      margin-left: 0.25rem;
      font-size: 1rem;
    }

    .error {
      background-color: #fdd;
      color: #a00;
      text-align: center;
      padding: 0.75rem;
      border-radius: 6px;
      margin-bottom: 1rem;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="image-section"></div>

    <div class="form-section">
      <div class="form-wrapper">
        <h2 id="formTitle">Login</h2>

        <% if (typeof error !== "undefined") { %>
          <div class="error"><%= error %></div>
        <% } %>

        <form id="authForm" method="POST" action="/login">
          <input type="email" name="email" placeholder="Email" required />
          <input type="password" name="password" placeholder="Password" required />
          <button type="submit">Continue</button>
        </form>

        <div class="toggle-container">
          <span id="toggleText">Don’t have an account?</span>
          <button onclick="toggleForm()" id="toggleBtn">Sign up</button>
        </div>
      </div>
    </div>
  </div>

  <script>
    function toggleForm() {
      const form = document.getElementById('authForm');
      const title = document.getElementById('formTitle');
      const toggleBtn = document.getElementById('toggleBtn');
      const toggleText = document.getElementById('toggleText');

      if (form.action.includes('/login')) {
        form.action = '/signup';
        title.innerText = 'Sign Up';
        toggleBtn.innerText = 'Log in';
        toggleText.innerText = 'Already have an account?';
      } else {
        form.action = '/login';
        title.innerText = 'Login';
        toggleBtn.innerText = 'Sign up';
        toggleText.innerText = 'Don’t have an account?';
      }
    }
  </script>
</body>
</html>
