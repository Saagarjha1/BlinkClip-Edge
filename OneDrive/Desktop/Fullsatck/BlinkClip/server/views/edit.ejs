<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Highlight</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: Arial, sans-serif;
      background-color: #f9fafb;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
    }

    .container {
      background-color: #ffffff;
      padding: 1.5rem;
      border-radius: 10px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
      max-width: 640px;
      width: 100%;
    }

    h2 {
      font-size: 1.75rem;
      font-weight: bold;
      text-align: center;
      margin-bottom: 1rem;
    }

    form textarea {
      width: 100%;
      padding: 0.75rem;
      border: 1px solid #ccc;
      border-radius: 8px;
      resize: vertical;
      margin-bottom: 1rem;
      font-size: 1rem;
    }

    button[type="submit"] {
      width: 100%;
      background-color: #2563eb;
      color: white;
      border: none;
      padding: 0.75rem;
      border-radius: 8px;
      font-size: 1rem;
      cursor: pointer;
      transition: background-color 0.2s ease;
    }

    button[type="submit"]:hover {
      background-color: #1d4ed8;
    }

    .cancel-link {
      display: block;
      text-align: center;
      margin-top: 1rem;
      font-size: 0.875rem;
      color: #2563eb;
      text-decoration: none;
    }

    .cancel-link:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <div class="container">
    <h2>Edit Highlight</h2>

    <form method="POST" action="/clip/edit/<%= clip._id %>">
      <textarea name="text" required rows="6"><%= clip.text %></textarea>
      <button type="submit">Save Changes</button>
    </form>

    <a href="/dashboard" class="cancel-link">Cancel & Go Back</a>
  </div>

</body>
</html>
